# Журнал разработки: генерация кандидатов для поиска услуг

Обновлено 20 сентября 2026 года. Метрика — macro Recall@50 на dev (2 452 запроса). Полные CI по срезам — [reports/experiments.md](reports/experiments.md), решения — [reports/decisions.json](reports/decisions.json).

## Состояние

- **Лучший принятый:** EXP-022 `core` = EXP-019 + переходы + `nb_cos` + `geo-damping`. Среднее трёх seed на CPU **0,91589**, полнота пула 0,9778.
- **Лучшая отправка:** EXP-022 `core_s42`, платформа **0,915389**. Использованы все 7 попыток, зачёт идёт по лучшей.
- **Последняя отправка:** ранговый ансамбль трёх seed EXP-022, dev 0,91581, платформа 0,915279 — та же точка, что и одиночный seed 42.
- **Итог:** **0,915389**, отправка 5.
- **Отклонено платформой:** EXP-023 = тот же набор при глубине 8 и 1000 деревьях. Dev 0,91612 (+0,15), срез без центра +1,09, платформа **0,905377** (−1,00). Ёмкость больше не трогаем.
- **Промежуточный:** EXP-019 `e5ft_user_s42`, dev 0,91110. Ответ `009_20260920-002852_catboost_e5ft_user` сгенерирован, не отправлялся.
- **Сдача:** 21 сентября 2026 года.

| Шаг | Dev R@50 | Полнота пула | Платформа |
| --- | ---: | ---: | ---: |
| BM25 + E5, RRF | 0,8087 | 0,9395 | — |
| `B0`: CatBoost на том же пуле | 0,8396 | 0,9395 | 0,827406 |
| EXP-001: резервный центр локации | 0,8736 | 0,9395 | 0,857622 |
| EXP-003: радиус 25 км × 50 | 0,8868 | 0,9618 | 0,873224 |
| EXP-006: микрокатегории соседей | 0,8930 | 0,9618 | — |
| EXP-009: пул 400 + 300 + 50 | 0,9014 | 0,9680 | 0,885611 |
| EXP-020: дообученный E5 | 0,9077 | 0,9778 | — |
| **EXP-022 `core_s42`: переходы, `nb_cos`, `geo-damping`** | **0,91467** | **0,9778** | **0,915389** |

Направление изменения dev и платформы совпало во всех четырёх отправках улучшений.

| Шаг | Прирост dev | Прирост платформы |
| --- | ---: | ---: |
| EXP-001 | +3,40 | +3,02 |
| EXP-003 | +1,32 | +1,56 |
| EXP-009 | +1,46 | +1,24 |
| EXP-022 | +1,45 | **+2,98** |
| EXP-023 | +0,15 | **−1,00** |
| Ансамбль 3 seed | +0,11 | −0,01 |

Абсолютные значения не сравнимы: у dev и платформы разные корпус и запросы.

**Калибровка сломалась на EXP-022 (DEV-036).** По четырём отправкам `платформа ≈ 0,951 × dev + 0,029` с отклонением ≤0,0017, пятая точка в неё не ложится: dev вырос на 1,45 п.п., платформа на 2,98. Объяснение — в срезах: EXP-022 дал **+2,95 [+0,98; +5,04] на 374 dev-запросах без центра**, а на benchmark таких запросов 17,4% против 15,3% на dev, и история там берётся целиком, а не cross-fit. Прирост на этом срезе окупается на платформе примерно вдвое, поэтому оставшиеся шаги выбираем по нему, а не по среднему dev.

## Решение

- **Кандидаты.**
  - BM25 (стемминг Snowball; заголовок ×3, параметры, описание).
  - Dense-канал: E5 (`intfloat/multilingual-e5-base`, с EXP-020 дообученный `e5ft`). Документ: заголовок, 500 символов параметров, описание; предел 512 токенов. В запрос входят фильтры.
  - Каждый канал даёт списки global 400, та же локация 300 и радиус 25 км × 50. Списки объединяются через RRF; в среднем около 980 кандидатов.
- **Отбор.**
  - CatBoostRanker `YetiRank`, 500 деревьев, глубина 6, lr 0,05, без ранней остановки (протокол `v2`), 6 000 train-текстов.
  - Признаки (57): ранги и оценки каналов, текст, расстояние до центра локации, свойства объявления, распределение микрокатегорий по 40 близким историческим запросам, косинус и ранг USER-base, вероятности переходов между локациями, центр выбранных объявлений соседей (`nb_cos`), флаги ненадёжного центра.
- **История.**
  - Центр локации — медиана координат её объявлений. Если объявлений нет, берётся медиана выбранных из этой локации объявлений в train-истории.
  - Признаки для train считаются cross-fit по 5 группам текстов. Dev, test и внутренняя проверка в историю не входят.
  - На benchmark используется вся train-история.

## Протокол

- Train: 497 673 пары, корпус 344 825 объявлений. Split 80/10/10 по нормализованному тексту, seed 42. Dev и test — по 2 452 текста, один контекст на текст. Числа до DEV-005 относятся к старому dev.
- Разности ниже — в п.п. R@50, парный bootstrap по dev-запросам (5 000 повторов). Множественный подбор по dev в CI95 не учтён.
- **Принятие:** CI95 > 0, важные срезы не просели, стоимость приемлема. Неопределённый результат не меняет родителя.
- Шум обучения: sd по seed 42/43/44 — 0,09 п.п. (EXP-008, EXP-009). Спорные эффекты решаются по среднему трёх seed.
- Test не открывался. Открывается один раз для итоговой конфигурации.
- `selection_lab` на GPU: одна модель около 30 с, но R@50 ниже CPU примерно на 0,5 п.п. (`base` 0,906 против EXP-019 0,911). GPU сравнивается только с GPU, итоговая модель учится на CPU.

## Эксперименты

**Главный вывод:** рост пула без признаков, различающих новых кандидатов, не переходит в R@50. Так было в EXP-003 `r50x100_v2`, EXP-017 и EXP-021. Основные потери — при отборе: у EXP-020 поиск теряет 2,22 п.п., отбор 7,01 п.п.

**Второй вывод (DEV-035):** проверка признаков по одному скрывала сигнал. Переходы (EXP-002), `nb_cos` (EXP-016) и `geo-damping` (EXP-013) по отдельности не проходили CI, а вместе дают +1,36 п.п. Решения по EXP-002/013/016 ниже пересмотрены набором.

### Принято

| EXP | Изменение | Родитель | Dev R@50 | Δ [CI95] | Ключевые факты |
| --- | --- | --- | ---: | --- | --- |
| 000 `b0` | CatBoost на пуле RRF | RRF | 0,8396 | +3,10 [+2,22; +3,97] | Потери: поиск 6,05, отбор 9,99. Без расстояния −1,50. |
| 001 `a` | Резервный центр из истории | `b0` | 0,8736 | **+3,39 [+2,56; +4,25]** | 374 запроса без центра: 0,526 → 0,710. |
| 003 `r25x50` | Радиусные списки 25 км × 50 | 001 `a` | 0,8868 | **+1,33 [+0,51; +2,12]** | Пул 0,9395 → 0,9618. |
| 008 `v2_base` | 500 деревьев без ранней остановки | 003 | 0,8888 | +0,19 [−0,26; +0,66] | Принят как протокол, не как прирост: ранняя остановка обрывала обучение на 2–19 деревьях. |
| 006 `neighbors` | Микрокатегории 40 близких запросов | `v2_base` | 0,8930 | **среднее трёх seed +0,83 [+0,40; +1,27]** | Плюс на каждом seed. [Расчёт](reports/exp006_seeds.json). |
| 009 `pool400x300` | Пул 400 + 300 + 50 | 006 | 0,9014 | **+0,84 [+0,36; +1,34]** | Потери поиска 3,82 → 3,20. Обучение 76 мин. |
| 020 `e5ft` | Дообученный E5 во всех dense-списках и признаках | 009 | 0,9077 | **+0,63 [+0,02; +1,27]** | Пул 0,9778 при 978 кандидатах вместо 1 016. Потери поиска 3,20 → 2,22. Без центра −0,23 [−2,12; +1,64]. |
| 022 `core` | Переходы + `nb_cos` + `geo-damping` набором (DEV-035) | 019 | 0,91589 (3 seed) | +0,48 [−0,05; +1,01] | Принят по срезу и по платформе: без центра **+2,95 [+0,98; +5,04]**, платформа 0,885611 → **0,915389**. На GPU при равных условиях +0,88 [+0,22; +1,43]. seed: 0,91467 / 0,91843 / 0,91456. |

### Неопределённо

| EXP | Изменение | Родитель | Dev R@50 | Δ [CI95] | Ключевые факты |
| --- | --- | --- | ---: | --- | --- |
| 019 `e5ft_user` | Косинус и ранг USER-base (`enc2_sim`, `enc2_rank`) | 020 | 0,9111 | +0,34 [−0,06; +0,75] | `enc2_rank` — 4-й по важности. Позитивы другой локации +1,92 [+0,62; +3,36]. Принят внутри EXP-022, отдельные seed не считались. |
| 016 | Центр выбранных объявлений соседей (`nb_cos`) | 009 | 0,9039 | +0,25 [−0,25; +0,75] | К среднему трёх seed EXP-009 +0,34 [−0,06; +0,76]. |
| 013 | Флаги ненадёжного центра (`geo-damping`) | 009 | 0,9039 | +0,25 [−0,29; +0,79] | Без центра **+2,76 [+0,7; +4,9]**, с центром −0,20. |

### Не принято

| EXP | Изменение | Родитель | Dev R@50 | Δ [CI95] | Причина |
| --- | --- | --- | ---: | --- | --- |
| 001 `b` | Историческое расстояние и разброс | 001 `a` | 0,8748 | +0,13 [−0,18; +0,44] | Эффекта нет. |
| 002 | Вероятности переходов между локациями | 001 `a` | 0,8746 | +0,11 [−0,33; +0,53] | Эффекта нет. При `alpha=100` −0,16, просели позитивы своей локации. |
| 003 `r50x100_v2` | Радиус 50 км × 100 | `v2_base` | 0,8832 | −0,56 [−1,16; +0,04] | Пул +0,62, но без центра −3,28. |
| 004 `fields` | BM25 по полям | 003 | 0,8874 | +0,06 [−0,44; +0,56] | Эффекта нет. Стоит 67 с и около 2 ГБ RAM. |
| 004 `filters` | Признак фильтров | `fields` | 0,8800 | −0,74 [−1,26; −0,22] | Сбой протокола: осталось 2 дерева. Проверен заново как EXP-018. |
| 008 `n12000` | 12 000 train-текстов | `v2_base` | 0,8891 | +0,04 [−0,42; +0,48] | Эффекта нет, обучение ×3, RAM 14,6 ГБ. |
| 008 `depth8` | Глубина 8 | 009 | 0,8966 | −0,48 [−1,00; +0,05] | Потери отбора 6,66 → 7,14. |
| 011 `ndcg50` | Функция потерь NDCG@50 | 006, 3 seed | 0,8971 | −0,08 [−0,35; +0,19] | Однословные запросы −1,87. [Расчёт](reports/exp011_seeds.json). |
| 011 `logloss` | Поточечный Logloss (`selection_lab`) | 009, 3 seed | 0,8935 | −0,70 [−1,12; −0,28] | На GPU 0,8863, −1,42. Разброс по seed 0,7 п.п. |
| 010 `a` | E5-запрос без фильтров | 009 | 0,8959 | **−0,55 [−1,05; −0,07]** | Пул +0,24, но запросы с фильтрами −0,82. |
| 012 | Смешение с cross-encoder | 009 | 0,9012 | −0,02 [−0,58; +0,54] | Отдельно 0,5164. Как признак не проверялся. |
| 014 | Prior точного текста | 009 | 0,9035 | −0,26 [−0,74; +0,21] | Мерился на 2 452 отложенных контекстах с текстом в истории; dev таких не содержит. |
| 015 | Ранговый ансамбль моделей | 009 | 0,9008 | −0,06 [−0,49; +0,37] | Эффекта нет. |
| 017 | Список «регион → города» | 009 | 0,8973 | −0,41 [−0,86; +0,02] | Пул +0,47, потери отбора 6,66 → 7,54. |
| 018 | Совпадение фильтров с параметрами | 009 | 0,9012 | −0,02 [−0,47; +0,42] | Эффекта нет. RAM 16,4 ГБ. |
| 021 `geo50x350` | Гео-список со штрафом вместо локальных квот | 009 | 0,8921 | **−0,93 [−1,56; −0,31]** | Пул +1,14, потери отбора 6,66 → 8,73. Без центра −3,21. |

### Диагностика без обучения CatBoost

| Проверка | Результат |
| --- | --- |
| EXP-020 `e5ft` (`encoder_diagnostics`) | Dense global@50 0,2806 → 0,3436, **+6,30 [+4,74; +7,82]**. AUC на 147 позитивах, потерянных отбором EXP-009: 0,554 → 0,653. Дообучение: 1 эпоха, 79 576 троек, 49 мин на GPU. |
| EXP-019 USER-base (`encoder_diagnostics`) | Как канал слабее E5: global@50 −3,25 [−4,84; −1,65]. Как признак: AUC на потерянных позитивах 0,628. |
| EXP-021 гео-пул (`geo_pool`) | Полнота 0,9680 → 0,9794 (+1,14) при 1 150 кандидатах. Контроль равного размера +0,16. |
| DEV-027 (`signal_diagnostics`) | Маркеры параметров (онлайн, выезд, прайс): AUC 0,49–0,52, закрыто. Смешанный алфавит: 1 запрос benchmark, закрыто. |
| EXP-016/018/013 отсев на пуле EXP-006 | +0,71…+0,77 у каждого. Контроль EXP-006 воспроизвёл 0,89304: дрейфа кода нет. |

### Отсев набором на GPU (DEV-035)

Пул и признаки EXP-019, `selection_lab`, 500 деревьев, seed 42/43/44. Δ — по среднему трёх seed. `base` — EXP-019 без проверяемых блоков.

[features_v1](configs/lab/features_v1.json): блок добавляется к `base`.

| Вариант | Среднее 3 seed | Δ к `base` [CI95] |
| --- | ---: | --- |
| `base` | 0,90616 | — |
| + `geo-damping` (EXP-013) | 0,91008 | +0,39 [+0,04; +0,77] |
| + `nb_cos` (EXP-016) | 0,91142 | +0,53 [+0,20; +0,87] |
| + переходы (EXP-002) | 0,91505 | **+0,89 [+0,44; +1,38]** |
| + историческое расстояние (EXP-001 `b`) | 0,90772 | +0,16 [−0,03; +0,37] |
| + BM25 по полям (EXP-004) | 0,90722 | +0,11 [−0,04; +0,27] |
| **все блоки** | **0,91979** | **+1,36 [+0,88; +1,90]** |

[features_loo](configs/lab/features_loo.json): блок убирается из «все блоки» (0,91986).

| Убран | Δ [CI95] | Решение |
| --- | --- | --- |
| переходы | **−0,49 [−0,81; −0,19]** | оставить |
| `nb_cos` | **−0,42 [−0,73; −0,14]** | оставить |
| `geo-damping` | −0,08 [−0,39; +0,23] | оставить: дешёвый, в EXP-013 +2,76 без центра |
| историческое расстояние | +0,13 [−0,03; +0,31] | убрать (`--geo-history fallback`) |
| BM25 по полям | +0,01 [−0,20; +0,21] | убрать: 65 с и около 2 ГБ RAM |

Итог — набор `core`. Ансамбль трёх seed к среднему seed то плюс, то минус (`base` +0,12, все блоки −0,11): шум.

### Проверки поверх `core` (DEV-036, DEV-037)

GPU, 3 seed, база — `core` 0,91989…0,92003 (пересчитывается в каждом лабе).

| Проверка | Δ к `core` [CI95] | Вывод |
| --- | --- | --- |
| `e5ft2`: второе дообучение E5, `--per-text 8`, 2× троек | +0,06 [−0,27; +0,40] | Отказ. Без центра −0,03. Совпадает с отказом от второго круга дообучения в стороннем решении. |
| Совпадение фильтров (EXP-018) | −0,04 [−0,18; +0,10] | Отказ подтверждён и в наборе. |
| Без `geo-damping` | −0,21 [−0,50; +0,09] | Оставляем в наборе. |
| 1000 деревьев | +0,18 [−0,00; +0,39] | Пограничный плюс, см. ёмкость. |
| Список «регион → города» (EXP-017) как пул | −0,25 [−0,61; +0,07] | Отказ, второй раз. |
| Радиус 100 км × 100 | −0,64 [−1,12; −0,16] | Отказ. |
| **Глубина 8** (EXP-008 `depth8`) | **+0,30 [+0,06; +0,55]** | Отклонялся при 44 признаках, с 57 признаками выигрывает. |
| **Глубина 8 + 1000 деревьев** | **+0,47 [+0,18; +0,77]** | 0,92477. Выбрано для финальной модели. |
| 1500 деревьев, глубина 6 | +0,19 [−0,06; +0,45] | Хуже, чем 1000 деревьев при глубине 8. |
| 1500 деревьев, lr 0,03 | +0,08 [−0,12; +0,29] | Замедление шага не помогает. |
| Гео-список со штрафом (EXP-021) | −0,19 [−0,71; +0,30] | Отказ, второй раз. |
| 12 000 train-текстов (EXP-008 `n12000`) | — | Не измерено: OOM (`exit 137`) при `MemoryMax=24G`. |

Потолок ёмкости найден: рост числа деревьев при глубине 6 упирается в +0,19, а глубина 8 с 1000 деревьями даёт +0,47 вдвое дешевле по времени. Дальше растить нечего.

Гео-пул менять не нужно: четыре попытки расширить географию (регионы, радиус 100 км, гео-список со штрафом дважды) проиграли уже с новыми признаками. Аналог иерархии гео из стороннего решения у нас реализован признаками переходов и `geo-damping`, а не списками кандидатов.

**Третий вывод:** «отклонено поодиночке» повторилось на ёмкости модели. Глубина 8 и 1000 деревьев отклонялись на бедном наборе признаков; с 57 признаками модель недообучена, и обе настройки дают плюс. Но действует это только на ёмкости: гео-пул и 12 000 текстов при богатых признаках не ожили.

### EXP-023: ёмкость отклонена платформой (DEV-038)

Глубина 8, 1000 деревьев, CPU, seed 42. Единственный случай, когда dev и платформа разошлись по знаку.

| | dev | без центра | платформа |
| --- | ---: | ---: | ---: |
| EXP-022 `core_s42` | 0,91467 | 0,8253 | **0,915389** |
| EXP-023 `d8_s42` | 0,91612 | 0,8362 | 0,905377 |

Dev +0,15 [−0,45; +0,74], целевой срез +1,09, платформа **−1,00**. Пул одинаковый (0,9778), различие целиком в отборе: среднее пересечение 50 позиций между ответами — 0,85, полностью совпали 4 запроса из 2 452.

Объяснение: исторические признаки на train считаются cross-fit по 5 группам, на benchmark — по всей истории, и там их распределение резче. Модель 8×1000 подгоняется под шкалу cross-fit-признаков плотнее, чем 6×500, и хуже переносит этот сдвиг. Вклад дал и отбор по dev: глубина 8 выбрана из дюжины вариантов на GPU по +0,47, а на CPU один seed дал +0,15 — часть выигрыша была шумом множественного подбора.

**Четвёртый вывод:** срез без центра — не универсальный предиктор платформы. Он объяснил успех EXP-022, но на EXP-023 вырос на 1,09 при падении платформы на 1,00. Прирост на срезе засчитывается только вместе с приростом среднего dev, а не вместо него. Ёмкость модели зафиксирована на 500 деревьях и глубине 6.

## Итог

Сдано **0,915389** (отправка 5, EXP-022 `core_s42`). Все 7 попыток использованы, зачёт по лучшей.

Что показали последние три отправки:

| Отправка | Что проверялось | Dev | Платформа |
| --- | --- | ---: | ---: |
| 5 | EXP-022 `core`: переходы, `nb_cos`, `geo-damping` | 0,91467 | **0,915389** |
| 6 | Ёмкость модели: глубина 8, 1000 деревьев | 0,91612 | 0,905377 |
| 7 | Ансамбль трёх seed EXP-022 | 0,91581 | 0,915279 |

Отправка 7 закрывает вопрос об ансамбле: dev +0,11, платформа −0,01, то есть ровно то же значение в пределах погрешности. Усреднение по seed не добавляет ничего сверх одиночной модели — это согласуется с лабораторными замерами, где ансамбль к среднему seed давал ±0,1 п.п.

**Чем кончилась история с dev.** Калибровка `платформа ≈ 0,951 × dev + 0,029` работала на четырёх отправках, сломалась на EXP-022 в плюс и на EXP-023 в минус. Итог: dev надёжен для выбора признаков (все принятые наборы подтвердились платформой), но не для выбора ёмкости модели и не для различения эффектов меньше 0,3 п.п.

**Что осталось непроверенным.** Доля города в выборах региона как признак (единственная гипотеза, которая била в срез без центра и не была реализована), 12 000 train-текстов при богатых признаках (не влезли в память), канал похожих запросов, cross-encoder как признак.

## Этапы

| DEV | Что сделано |
| --- | --- |
| 001–002 | Журнал; [EDA](notebooks/eda.ipynb): 90,41% ID benchmark нет в train, у 427 запросов нет объявлений своей локации. |
| 003–005 | Split, Recall@50, BM25, E5, RRF. В DEV-005 исправлена выборка dev/test. |
| 006–008 | Инференс benchmark и валидатор CSV; `B0`; единый запуск экспериментов, карта потерь. |
| 009–011 | Cross-fit исторических признаков; резервный центр (EXP-001) в обучении и инференсе. |
| 012–014 | Переходы (EXP-002) отклонены, радиус (EXP-003) принят, BM25 по полям (EXP-004) отклонён. |
| 015–017 | Сводка экспериментов с CI по срезам; протокол `v2`; отклонены радиус 50 км и 12 000 текстов. |
| 018–020 | Микрокатегории (EXP-006) приняты, NDCG отклонён, глубокий пул (EXP-009) принят, пилот cross-encoder. |
| 021–026 | Реализованы и отклонены: `geo-damping`, prior точного текста, E5 без фильтров, ансамбль. Снижен расход RAM. |
| 027–029 | Диагностика сигналов без обучения; признаки EXP-016/017/018; ночная очередь: seed EXP-009, контроль дрейфа. |
| 030 | `embed_corpus --model`, `encoder_diagnostics`, `finetune_dense`. |
| 031 | `selection_lab`: признаки строятся один раз, много вариантов CatBoost в одном процессе. Logloss отклонён. |
| 032 | Гео-списки со штрафом (`geo_pool`); диагностика `e5ft` и гео-пула. |
| 033 | EXP-021 отклонён. |
| 034 | EXP-020 `e5ft` принят: флаг `--dense-model`, модель восстанавливается в `predict`; признаки второго энкодера (`--second-encoder`). |
| 035 | Отсев набором в `selection_lab`: `features_v1` и leave-one-out, набор `core`; ночная очередь EXP-022. |
| 036 | EXP-022 принят и отправлен (0,915389). Отклонены `e5ft2`, регионы и радиус 100 км. Глубина 8 пересмотрена и выигрывает. |
| 037 | Лабы ёмкости, гео и объёма данных. Гео-список отклонён повторно, 12 000 текстов не измерены (OOM). |
| 038 | EXP-023 (глубина 8, 1000 деревьев) отправлен и отклонён платформой: 0,905377 при росте dev. Ёмкость зафиксирована на 6×500. |
| 039 | Ансамбль трёх seed EXP-022 отправлен седьмой попыткой: 0,915279. Итог задания — 0,915389. |

История коммитов: `git log --oneline -- DEVELOPMENT_LOG.md`.

## Отправки

| # | `data/output/` | Коммит | Dev | Платформа | SHA-256 `answer.csv` |
| --- | --- | --- | ---: | ---: | --- |
| 1 | `002_20260917-213857_catboost` | `0e5022a` | 0,8396 | 0,827406 | `f777c2b8d849062073d87c7a3e4e21ced56b657e7f9ec892e35f84ad73b26926` |
| 2 | `005_20260918-021512_catboost_geohist` | `6287176` | 0,8736 | 0,857622 | `1bd8358e16ba62132a0ef53a85d2208b33e2ea82586abc618bbc6e726f22b954` |
| 3 | `006_20260918-133936_catboost_radius` | `da96c4d` | 0,8868 | 0,873224 | `712525fae6614dc6896d891c85b1cea137864579472f1e906d47764aed1b1995` |
| 4 | `008_20260918-220834_catboost_pool400` | `3f253cc` | 0,9014 | 0,885611 | `d9619cae9b62fd75e5d8ffc0956513e009cb68cef3d246ad55d18522c1154498` |
| 5 | `010_20260920-025358_catboost_exp022_s42` | `bae5f15` | 0,91467 | **0,915389** | `c04e5d3f0e8311fa225a6bb9f33af877bb4da6f4fa4a2bdb7c8a01262e3876f3` |
| 6 | `011_20260920-163245_catboost_exp023_d8_s42` | `6f5a94c` | 0,91612 | 0,905377 | `a57a946a3da5434f1a7e33b19072b8b895dc208b88ca277637fd38e87b58e2b1` |
| 7 | `012_20260920-173423_ensemble_exp022` | `6f5a94c` | 0,91581 | 0,915279 | `44f54ff2489e4a61591d74619ad4386c19a1189f346f7f0b6fcffb6e3008cb3d` |

Повторная генерация из готовых артефактов воспроизводит ответ побайтно (`003` = `002`, `007` = `008`, `013` = `012`). `meta.json` фиксирует конфигурацию, коммит и хэши входов, модели и ответа. Полный пересчёт с нуля не проверялся.

Гарантия проверена опытом, а не доказана кодом: в `signals.text_item_means` порядок строк даёт `.unique()` без `maintain_order`, а суммирование идёт через `index_add_` по float32 на устройстве dense-канала. Перестановка слагаемых в принципе меняет младшие разряды. На одной машине порядок стабилен, три пары прогонов дали совпадающие SHA-256, но на другом железе или другой версии Polars совпадение не гарантировано. Пересечение слов (`features.overlap`) от порядка не зависит: там считается только размер пересечения.

```bash
uv sync --all-groups --locked
HF_HUB_OFFLINE=1 uv run python -m candgen.scripts.predict --method catboost \
  --model data/artifacts/experiments/EXP-022/core_s42/model.cbm --tag catboost_exp022_s42
```

## Ограничения

- У benchmark чаще пустые фильтры (63% против 48% на dev) и категория `0` (222 запроса против одного).
- Разметка поведенческая: невыбранные объявления могут быть релевантны.
- 35,5% документов длиннее 512 токенов E5.
- У 94 из 427 benchmark-запросов без центра разброс истории больше 100 км: резервный центр там шумный.
