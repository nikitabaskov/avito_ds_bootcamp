# Журнал разработки: генерация кандидатов для поиска услуг

Обновлено 19 сентября 2026 года. Численные отчёты по запускам и срезам — в [reports/experiments.md](reports/experiments.md).

## Результат

**Текущий вариант — EXP-009 `pool400x300_s42` (DEV-020).** Dev macro Recall@50 **0,90139**, полнота пула **0,96803**. Отправка `008_20260918-220834_catboost_pool400/answer.csv` дала **0,885611** на платформе.

| Конфигурация | Dev R@50 | Полнота пула | Платформа |
| --- | ---: | ---: | ---: |
| BM25 + E5, глобальные и локальные списки, RRF | 0,8087 | 0,9395 | — |
| `B0`: тот же пул, CatBoost | 0,8396 | 0,9395 | 0,827406 |
| EXP-001 `a`: резервный центр локации | 0,8736 | 0,9395 | 0,857622 |
| EXP-003 `r25x50`: радиусные списки | 0,8868 | 0,9618 | 0,873224 |
| EXP-008 `v2_base`: 500 деревьев | 0,8888 | 0,9618 | — |
| EXP-006 `neighbors`: микрокатегории, seed 42 | 0,8930 | 0,9618 | — |
| **EXP-009 `pool400x300_s42`: расширенный пул** | **0,9014** | **0,9680** | **0,885611** |

Платформа и dev различаются корпусом и запросами; сравнивать их абсолютные значения нельзя. Для трёх отправленных улучшений направление изменения совпало. `EXP-009` превысил EXP-003 на dev на 1,46 п.п., на платформе — на 1,24 п.п.

На исправленном dev добавление E5 к BM25 с глобальными и локальными списками повысило RRF с 0,7760 до 0,8087 (+3,27 п.п.). CatBoost на том же пуле дал 0,8396: +3,10 п.п. к RRF, CI95 [+2,22; +3,97]. Эти измерения обосновали два канала и обучаемый отбор.

## Решение и протокол

BM25 с русским стеммингом ищет по заголовку ×3, параметрам и описанию. `intfloat/multilingual-e5-base` (`d128750`, без дообучения) ищет по смыслу; документ содержит заголовок, первые 500 символов параметров и описание, предел — 512 токенов. Для каждого канала EXP-009 берёт 400 глобальных, 300 той же локации и 50 объявлений в радиусе 25 км. Списки объединяются через RRF; CatBoostRanker выбирает 50 ID из пула (в среднем 1 016 объявлений на dev). Признаки включают ранги и оценки каналов, текст, географию, свойства объявления и распределение микрокатегорий по 40 близким историческим запросам. Глобальный поиск сохраняет объявления соседних локаций.

Расстояние считается до медианы координат объявлений локации запроса. Если её нет, используется медиана координат объявлений, выбранных из этой локации в разрешённой train-истории. Это ориентир спроса, не координаты пользователя. Для обучающих текстов исторические признаки строятся по пяти группам с исключением всей собственной текстовой группы; dev/test и группы внутренней проверки в историю не входят. На benchmark допустима вся предоставленная train-история без скрытой разметки.

Train содержит 497 673 положительные пары; локальный корпус — 344 825 уникальных объявлений, benchmark — 2 452 запроса и 189 212 объявлений. Split 80/10/10 по нормализованному тексту, seed 42. В dev и test по 2 452 текста, один контекст на текст; повторы позитивов по полному контексту удалены. Метрика — средняя по запросам доля известных позитивов среди 50 ответов. В DEV-005 исправлен порядок выборки текста и контекста: числа DEV-003/004 на старом dev нельзя сравнивать с текущими.

Основное обучение: 6 000 выбранных train-текстов, 500 деревьев `YetiRank`, глубина 6, `learning_rate=0.05`, seed 42, без ранней остановки (`v2`). В первой выборке `B0` обучались 5 093 группы после исключения запросов без позитивов в пуле; состав внутренней проверки зафиксирован. Для разностей ниже использован парный bootstrap по 2 452 dev-запросам: 5 000 повторов, seed 42. CI95 относится к этому split и указанным seed; множественный подбор по dev им не учитывается. Положительный CI95 вместе с сохранением важных срезов и приемлемой стоимостью — критерий принятия. Для результатов в пределах шума родитель не меняется.

## Эксперименты: измерение и решение

Разности указаны в процентных пунктах R@50 к указанному родителю. `v1` использует раннюю остановку, `v2` — фиксированные 500 деревьев. Полные CI по срезам и полноте пула находятся в [сводке](reports/experiments.md); решения для неё зафиксированы в [reports/decisions.json](reports/decisions.json).

| Эксперимент | Родитель; dev R@50 | Разность [CI95] | Решение и доказательство |
| --- | --- | --- | --- |
| EXP-000 `b0` | база; 0,8396 | — | Воспроизведён DEV-007; полнота пула 0,9395. Потери: поиск 6,05 п.п., отбор 9,99 п.п. У 374 запросов без центра R@50 0,5263. |
| EXP-000 `no_dist` | `b0`; 0,8247 | −1,50 [−2,27; −0,75] | Диагностика: расстояние оставлено. |
| EXP-001 `a` | `b0`; 0,8736 | **+3,39 [+2,56; +4,25]** | Принят резервный центр. Без центра: 0,5263 → 0,7104 на 374 запросах; платформа 0,827406 → 0,857622. Пул тот же. |
| EXP-001 `b` | `a`; 0,8748 | +0,13 [−0,18; +0,44] | Дополнительные исторические расстояние и разброс не приняты: общего эффекта нет. |
| EXP-002 `prob_a10` / `prob_a100` | `a`; 0,8746 / 0,8720 | +0,11 [−0,33; +0,53] / −0,16 [−0,55; +0,23] | Вероятности переходов между локациями не приняты: оба CI включают ноль; при `alpha=100` проседают позитивы своей локации. |
| EXP-003 `r25x50` | `a`; 0,8868 | **+1,33 [+0,51; +2,12]** | Принят радиус 25 км × 50. Полнота 0,9395 → 0,9618, средний пул 735 → 670; платформа 0,857622 → 0,873224. |
| EXP-003 `r50x100` (`v1`) | `a`; 0,8779 | +0,43 [−0,40; +1,27] | Не принят: модель остановилась на 19 деревьях; проверен повторно по `v2`. |
| EXP-003 `r50x100_v2` | `v2_base`; 0,8832 | −0,56 [−1,16; +0,04] | Не принят: полнота пула +0,62 [+0,33; +0,94], но отбор ухудшился. Без центра −3,28 [−5,53; −1,31], позитивы другой локации −3,15 [−5,13; −1,12]. |
| EXP-004 `fields` | `r25x50`; 0,8874 | +0,06 [−0,44; +0,56] | Отдельные BM25 заголовка, параметров и описания не приняты. При равных 1 000 деревьях эффект к базе −0,06 [−0,56; +0,46]; дополнительные индексы стоят 67 с и ~2 ГБ RAM. |
| EXP-004 `filters` | `fields`; 0,8800 | −0,74 [−1,26; −0,22] | Не принят. Ранняя остановка оставила 2 дерева; это сбой протокола обучения, поэтому не доказано, что сам признак фильтров вреден. |
| EXP-008 `trees_base` / `trees_fields` | `r25x50`; 0,8898 / 0,8891 | +0,29 [−0,24; +0,84] / +0,23 [−0,32; +0,79] | Диагностика при 1 000 деревьях: dev вышел на плато после 50 деревьев; полевые оценки не добавили качества. Шум внутренней проверки делал раннюю остановку ненадёжной. |
| EXP-008 `v2_base` | `r25x50`; 0,8888 | +0,19 [−0,26; +0,66] | Принят как фиксированный протокол сравнения, не как доказанный прирост качества: 500 деревьев без ранней остановки. |
| EXP-008 `v2_seed43` / `v2_seed44` | `v2_base`; 0,8896 / 0,8906 | +0,08 [−0,31; +0,47] / +0,18 [−0,22; +0,59] | Оценён шум обучения: sd метрики по seed 42/43/44 — 0,09 п.п.; seed по максимуму dev не выбирается. |
| EXP-008 `n12000` | `v2_base`; 0,8891 | +0,04 [−0,42; +0,48] | Не принят: удвоение текстов дало нулевой эффект при 32 вместо ~11 минут обучения и пике RAM 14,6 ГБ. 30 000 текстов сняты с плана. |
| EXP-006 `neighbors` / `neighbors_seed43` / `neighbors_seed44` | `v2_base` с теми же seed; 0,89304 / 0,89854 / 0,90212 | +0,426 / +0,898 / +1,151; **средняя +0,825 [+0,399; +1,269]** | Приняты признаки микрокатегорий. Среднее R@50 0,89790 против 0,88965 у базы; прирост есть на трёх seed, сильнее на запросах с центром. Это среднее метрик, не ансамбль. Для последующих пар выбран seed 42. [Расчёт](reports/exp006_seeds.json). |
| EXP-011 `ndcg50_s42` / `ndcg50_s43` / `ndcg50_s44` | EXP-006 с теми же seed; 0,89628 / 0,89532 / 0,89974 | +0,324 / −0,322 / −0,238; средняя −0,079 [−0,352; +0,190] | NDCG@50 не принят: среднего выигрыша нет; однословные запросы −1,871 [−3,976; −0,306]. [Расчёт](reports/exp011_seeds.json). |
| EXP-009 `pool400x300_s42` | EXP-006 seed 42; **0,90139** | **+0,836 [+0,360; +1,339]** | Принят более глубокий текстовый пул 400+300+50. Полнота +0,63 [+0,33; +0,95], потери поиска 3,82 → 3,20 п.п., отбора 6,87 → 6,66. Обучение 76 минут; платформа **0,885611**. |
| EXP-010 `a_s42` | EXP-009; 0,8959 | **−0,55 [−1,05; −0,07]** | E5-запрос без фильтров не принят. Dense global +2,72 [+1,57; +3,89] и полнота пула +0,24 [0,00; +0,52], но потери отбора +0,79 п.п.; на запросах с фильтрами −0,82 [−1,68; −0,02]. |
| EXP-012: пилот cross-encoder | EXP-009; смешение 0,9012 | −0,02 [−0,58; +0,54] | Ранговое смешение не принято; чистый cross-encoder дал 0,5164. При этом он ставит в свой top-50 59 из 185 позитивов, потерянных отбором EXP-009. Это основание отдельно проверить его оценки как признаки CatBoost. |
| EXP-014: prior точного текста | EXP-009; 0,9035 на отдельной выборке | −0,26 [−0,74; +0,21] | Не принят. Сравнение на 2 452 отложенных контекстах с текстом в истории: базовая модель 0,9061, жёсткий prior 0,9035. Dev таких текстов по split не содержит; выборка слегка знакома модели через признаки чужих запросов. |
| EXP-015: ансамбль | EXP-009; 0,90076 | −0,06 [−0,49; +0,37] | Ранговый RRF EXP-009 и EXP-006 seed 43/44 не улучшил лучшую модель. Два seed EXP-006 дали 0,89793: +0,49 к seed 42, но −0,42 к лучшему seed 44. |
| EXP-016/018/013 `screen_s42` | EXP-006 seed 42; 0,90072 / 0,90008 / 0,90031 | +0,77 [+0,26; +1,29] / +0,71 [+0,29; +1,15] / +0,73 [+0,27; +1,22] | Отсев на пуле EXP-006. Одинаковый прирост трёх разных признаков проверен контролем `EXP-006/control_s42` на текущем коммите: 0,89304 — ровно как у EXP-006, дрейфа кода нет. Эффекты сосредоточены в целевых срезах: фильтры +1,04 на запросах с фильтрами, geo-damping +3,16 [+1,20; +5,30] без центра. Все три прошли на полный пул. |
| EXP-009 `pool400x300_s43` / `_s44` | EXP-009 seed 42; 0,90044 / 0,89958 | −0,10 [−0,58; +0,37] / −0,18 [−0,61; +0,22] | Шум обучения EXP-009: среднее трёх seed 0,90047, sd 0,09 п.п.; seed 42 выше среднего на 0,09 п.п. |
| EXP-016 `pool400_s42` | EXP-009; 0,90391 | +0,25 [−0,25; +0,75] | Центр выбранных объявлений соседей (`nb_cos`, 4-й по важности). К среднему трёх seed EXP-009 +0,34 [−0,06; +0,76]. Неопределённый, не принят: равномерный небольшой плюс по срезам. |
| EXP-013 `pool400_s42` | EXP-009; 0,90391 | +0,25 [−0,29; +0,79] | Флаги ненадёжного исторического центра. К среднему трёх seed +0,34 [−0,09; +0,80]. Неопределённый, не принят; целевой срез без центра **+2,76 [+0,7; +4,9]**, с центром −0,20 [−0,7; +0,3]. |
| EXP-018 `pool400_s42` | EXP-009; 0,90115 | −0,02 [−0,47; +0,42] | Совпадение фильтров с параметрами не принято: на глубоком пуле сигнал не добавляет качества. Пик RAM 16,4 ГБ. |
| EXP-017 `region200_s42` | EXP-009; 0,89730 | −0,41 [−0,86; +0,02] | Список «регион → города» не принят. Полнота пула +0,47 п.п. (0,9727), но потери отбора 6,66 → 7,54 п.п.; без центра −0,15 [−2,1; +1,8]. Третье подтверждение: рост пула без признаков, различающих новых кандидатов, не переходит в R@50. |
| EXP-008 `depth8_pool400` | EXP-009; 0,89662 | −0,48 [−1,00; +0,05] | Глубина 8 не принята: потери отбора 6,66 → 7,14 п.п. Ёмкость модели не ограничивает отбор. |
| EXP-011 `logloss_cpu` / `logloss_gpu` (`selection_lab`) | среднее трёх seed EXP-009 (0,90047); 0,89347 / 0,88627 | −0,70 [−1,12; −0,28] / −1,42 [−1,94; −0,93] | Поточечный Logloss вместо YetiRank не принят, среднее по seed 42/43/44. Разброс по seed 0,7 п.п. против 0,09 у YetiRank; ансамбль вероятностей трёх seed на CPU 0,89730, −0,32 [−0,81; +0,16] — +0,38 к среднему seed, но ниже эталона. GPU хуже CPU на 0,7 п.п. при той же функции потерь. Обучение 3 мин на CPU, 15 с на GPU против 75 мин YetiRank CPU. |

EXP-009 оставляет **3,20 п.п. потерь до пула и 6,66 п.п. при отборе**. Поэтому дальнейшие измерения сосредоточены на отборе. Дополнительные кандидаты полезны лишь тогда, когда модель удерживает их в top-50: это показало сравнение EXP-003 `r50x100_v2` и EXP-009.

## Реализовано без доказанного прироста и следующие шаги

| Направление | Состояние и основание |
| --- | --- |
| EXP-005, символьный TF-IDF | Не реализован. Возможный сигнал для опечаток; доля таких ошибок не измерена, поэтому низкий приоритет. |
| EXP-007, выборка негативов | Не реализована. Цель — стоимость обучения; принимать только при измеренной экономии и допустимой потере Recall. |
| EXP-010 `b/c`, больше текста E5 | Не запускались: требуют пересчёта эмбеддингов корпуса, эффекта для этой серии пока не показано. |
| EXP-016 + EXP-013 вместе | Не запускалось. По отдельности +0,25 п.п. с CI через ноль; признаки бьют в разные срезы. Проверять на seed 42/43/44 против трёх seed EXP-009, как EXP-006; ~4 часа обучения. |
| EXP-012 `ce_s42`, cross-encoder как признак | Не реализован. Пилот слабый (0,5164 отдельно); ниже по приоритету, чем энкодеры. |
| Закрыто диагностикой DEV-027 | Маркеры параметров (онлайн-запись, «Удалённо», «Онлайн», выезд, прайс-лист): AUC 0,49–0,52. Смешанная латиница/кириллица: 1 запрос benchmark из 2 452 — нормализация не нужна. |

**Следующие шаги (после DEV-029).** Ночь показала предел текущего представления текста: признаки поверх E5-base дают не больше +0,3 п.п., расширение пула не удерживается отбором. Для платформенного 0,9 нужно около +1,5 п.п. к 0,885611, поэтому следующая работа — качество самих векторов.

1. **EXP-019: второй энкодер `deepvk/USER-base`** (`e844647`) без дообучения. Эмбеддинги локального корпуса (345 тыс.) и benchmark (189 тыс.) на GPU, затем `encoder_diagnostics` без обучения CatBoost: recall dense-списков USER против E5 на глубинах 50–400, полнота пула EXP-009 с добавленными списками USER (контроль — столько же кандидатов из более глубокого E5 global 800), AUC `sim` внутри top-200 и доля потерянных позитивов в top-50 по `sim` (как в DEV-027). CatBoost на пуле EXP-009 с новыми списками и признаком сходства — только при сигнале. Основание: во внешних решениях второй энкодер (USER-base, EmbeddingGemma) давал +0,8 п.п. к одному E5.
2. **EXP-020: дообучение E5 на парах train с трудными негативами** (`finetune_dense`). Контрастивная функция потерь с негативами внутри батча и одним трудным негативом на запрос: объявление той же локации с рангов 10–100 исходного E5, не выбранное ни в одном контексте этого текста. До 4 контекстов на текст; батч без повторов текста и объявлений. Из обучения исключены 6 000 текстов выборки CatBoost (включая группы внутренней проверки), чтобы признаки dense на обучении CatBoost оставались как на новых запросах; dev/test не входят по split. Холостой прогон: 52 956 текстов, 79 576 троек, 2 486 шагов по 32; 1,1 с на шаг, пик GPU 5,8 ГБ — около 46 минут. Оценка тем же `encoder_diagnostics`, затем полный CatBoost на dev. Это меняет решение раздела 5.4 PROJECT_PLAN (дообучение вне обязательного объёма): основание — потери отбора и поиска не снижаются признаками поверх исходного E5. Во внешних решениях dense recall@50 рос 0,826 → 0,874 после дообучения, на новых текстах сильнее, чем на знакомых.

Оба шага тяжёлые (GPU, десятки минут на корпус, часы на дообучение) и запускаются пользователем.

Перед новой отправкой лучшего кандидата проверить seed 43/44 (у EXP-009 проверены). Затем выбрать конфигурацию по dev, **один раз** открыть независимый test, сгенерировать финальный CSV и проверить воспроизводимость. Подбор по test и по числу платформенных попыток не проводить. README требует инструкции запуска и состава локальных артефактов.

## Этапы разработки

| Этап | Завершённая работа |
| --- | --- |
| DEV-001 | Введён нумерованный журнал и связь этапов с коммитами. |
| DEV-002 | [EDA](notebooks/eda.ipynb): 90,41% ID benchmark не встречались в train; у 427 запросов нет объявлений своей локации. Выбран поиск по содержанию с мягкой географией. |
| DEV-003 | Split, Recall@50, BM25. На старом dev повтор заголовка и фильтры дали 0,3110 → 0,3398; лемматизация не превзошла стемминг. После DEV-005 абсолютные числа не сравниваются с текущими. |
| DEV-004 | E5, локальные списки, RRF; старый dev, результаты заменены после DEV-005. |
| DEV-005 | Исправлена выборка dev/test по текстам; повторена базовая оценка. |
| DEV-006 | Инференс benchmark, валидатор CSV и метаданные ответа. |
| DEV-007 | Базовый CatBoost `B0`; чистое воспроизведение CSV `002` → `003`. |
| DEV-008 | Единый запуск экспериментов и карта потерь `B0`. |
| DEV-009 | Cross-fit исторических признаков; диагностика `no_dist`. |
| DEV-010 | Принят резервный центр EXP-001 `a`. |
| DEV-011 | Исторические признаки в benchmark-инференсе по всей train-истории. |
| DEV-012 | Проверены, не приняты вероятности переходов EXP-002. |
| DEV-013 | Приняты радиусные списки EXP-003 `r25x50`. |
| DEV-014 | Проверены, не приняты полевые BM25-оценки EXP-004. |
| DEV-015 | Сводка экспериментов, CI по срезам и кривая качества по деревьям. |
| DEV-016 | Зафиксирован протокол `v2`; повтор EXP-004 подтвердил отсутствие эффекта. |
| DEV-017 | Не приняты широкий радиус `r50x100_v2` и 12 000 текстов `n12000`. |
| DEV-018 | Реализованы признаки микрокатегорий EXP-006 с cross-fit. |
| DEV-019 | EXP-006 принят по трём seed; NDCG@50 отклонён; подготовлен EXP-009. |
| DEV-020 | Принят глубокий пул EXP-009; реализован пилот cross-encoder. |
| DEV-021 | Реализованы, пока не измерены признаки `geo-damping` EXP-013. |
| DEV-022 | Реализован prior точного текста EXP-014; dev не покрывает точные дубликаты. |
| DEV-023 | Добавлен E5-запрос без фильтров и сравнение полноты пулов EXP-010. |
| DEV-024 | Реализован ансамбль EXP-015 и общий расчёт признаков для dev/benchmark. |
| DEV-025 | E5 без фильтров отклонён после обучения; снижено потребление RAM перед CatBoost. |
| DEV-026 | Prior точного текста отклонён на отдельной выборке повторяющихся текстов. |
| DEV-027 | Диагностика сигналов без обучения (`signal_diagnostics`, `core/signals.py`): AUC признаков внутри top-200 EXP-009, полнота пула для новых списков. Реализованы признаки EXP-016 (центр выбранных объявлений соседей) и EXP-018 (совпадение фильтров); закрыты маркеры параметров и нормализация смешанного алфавита. |
| DEV-028 | Список кандидатов EXP-017 `dense_region` для запросов без центра локации (`--region-k`, восстанавливается `predict` из `model.json`). |
| DEV-029 | Ночная очередь из восьми запусков: контроль EXP-006 воспроизвёл 0,89304; seed 43/44 EXP-009 — sd 0,09 п.п.; EXP-016 и EXP-013 +0,25 п.п. (неопределённо), EXP-018, EXP-017 и глубина 8 не приняты. Следующие шаги — второй энкодер и дообучение E5. |
| DEV-030 | Реализованы, пока не измерены: выбор модели в `embed_corpus` (`--model`, закреплённые ревизии), `encoder_diagnostics` для EXP-019/020 и `finetune_dense` для EXP-020 (`core/finetune.py`). |
| DEV-031 | Лаборатория отбора `selection_lab`: признаки EXP-009 строятся один раз, затем в одном процессе обучаются варианты CatBoost с bootstrap против среднего трёх seed EXP-009 и ансамблем seed. `experiment.py` разделён на `build_parser`/`configure`/`prepare`/`train_frames`; в `ranker` добавлен поточечный Logloss. Logloss отклонён (EXP-011). |

История коммитов: `git log --oneline -- DEVELOPMENT_LOG.md`.

## Отправки и воспроизводимость

| Отправка | Файл в `data/output/` | Dev | Платформа | SHA-256 `answer.csv` |
| --- | --- | ---: | ---: | --- |
| 1 | `002_20260917-213857_catboost` (`003` побайтно совпадает, коммит `0e5022a`) | 0,8396 | 0,827406 | `f777c2b8d849062073d87c7a3e4e21ced56b657e7f9ec892e35f84ad73b26926` |
| 2 | `005_20260918-021512_catboost_geohist` (коммит `6287176`) | 0,8736 | 0,857622 | `1bd8358e16ba62132a0ef53a85d2208b33e2ea82586abc618bbc6e726f22b954` |
| 3 | `006_20260918-133936_catboost_radius` (коммит `da96c4d`) | 0,8868 | 0,873224 | `712525fae6614dc6896d891c85b1cea137864579472f1e906d47764aed1b1995` |
| 4 | `008_20260918-220834_catboost_pool400` (`007` побайтно совпадает, коммит `3f253cc`) | 0,9014 | **0,885611** | `d9619cae9b62fd75e5d8ffc0956513e009cb68cef3d246ad55d18522c1154498` |

`008/meta.json` фиксирует конфигурацию, commit, хэши входных Parquet, модели и ответа. CSV содержит 2 452 строки по 50 уникальных ID; валидатор проверяет схему, запросы и принадлежность объявлений benchmark-корпусу. Повторная генерация из готовых артефактов воспроизводит ответ; полный пересчёт с нуля на GPU отдельно не проверялся. `data/` исключён из Git, поэтому для запуска нужны исходные Parquet, скачанные веса E5 и локальные артефакты.

```bash
uv sync --all-groups --locked
HF_HUB_OFFLINE=1 uv run python -m candgen.scripts.predict --method catboost \
  --model data/artifacts/experiments/EXP-009/pool400x300_s42/model.cbm \
  --tag catboost_pool400
```

Ограничения оценки: у benchmark чаще пустые фильтры (63% против 48% на dev) и категория `0` (222 запроса против одного); разметка отражает выбор пользователя, поэтому невыбранные объявления могут быть релевантны. Около 35,5% проверенных документов длиннее лимита E5 в 512 токенов. У 94 из 427 benchmark-запросов без исходного центра разброс исторически выбранных объявлений превышает 100 км; резервный центр там может быть шумным.
